<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nprcommunity.npronecommunity">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Navigate"
            android:label="@string/title_activity_navigate"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <service
            android:name=".Background.BackgroundAudioService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".ChannelSelect" />

        <activity
            android:name=".Search"
            android:label="@string/app_name"
            android:launchMode="singleTop">

            <!-- to identify this activity as "search_searchable" -->
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/search_searchable" />
        </activity>

        <activity android:name=".Station"
            android:label="@string/station"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/station_searchable" />
        </activity>


        <service
            android:name=".Store.CacheClearJobService"
            android:label="Word service"
            android:permission="android.permission.BIND_JOB_SERVICE" >
        </service>

    </application>

</manifest>